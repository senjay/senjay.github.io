# 安装过的包
```Shell
npm install //基础包
npm install hexo-deployer-git --save //git包
npm install hexo-renderer-kramed --save //数学包
npm install hexo-image-link --save //图片链接转换包
npm install --save hexo-helper-live2d //2d
```